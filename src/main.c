#include "image.h"
#include "filter.h"
#include <stdio.h>

int main() {
    // Image *image = create_image(800, 600);
    load_jpeg("img/img.jpeg");

    // apply_grayscale(image);
    // save_image(image, "output.bmp");

    // destroy_image(image);
    return 0;
}